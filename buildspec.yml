version: 0.1
phases:
  install:
   commands:
    - npm install -g serverless
    - npm install
  build:
   commands:
    - /usr/local/lib/node_modules/serverless/bin/serverless deploy --stage cicd | tee deploy.out
  post_build:
   commands:
    - npm test


    